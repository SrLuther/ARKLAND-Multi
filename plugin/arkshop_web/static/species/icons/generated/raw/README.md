# Icon sources (not shipped)

PNG sources used by `tools/generate_ai_species_icons.py`.

These files are **excluded from** `ARKLAND-WebStore.exe` / the installer
(`ARKLAND-WebStore.spec` skips the `raw/` and `demo/` directories). Runtime
serves only `../{species}.webp`.
