# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/

# Distribution / build
dist/
build/
installer/

# Virtual environment
.venv/
venv/
ENV/

# Python portátil local (não commitar — são 40-80MB)
.python/
.python-full/

# PyInstaller
*.manifest

# Build logs (gerados pelo build.bat)
build_*.txt

# Config gerado pelo app (dados do usuário)
# NÃO versionar — cada máquina tem o seu
# %APPDATA%\ARKLAND-Multi\config.json  ← fora do repo por natureza

# Segredos locais (tokens, chaves — nunca commitar)
.env
.env.local
.env.*.local
.secrets.local
*.secrets.local
# Evidência de host / dumps (podem conter admin_password, RCON, etc.)
.bug/
# Cópia local de edição do config (ex.: Tags/licenças — apply manual pelo host)
.edit/

# VS Code
.vscode/

# Nexus Sync (ferramenta interna do editor)
.nexus-sync/

# Windows
Thumbs.db
Desktop.ini

# Python eggs
*.egg-info/
.eggs/

beacon_token.json
*_out.txt
*_err.txt
_tmp_fix.py
_tmp_*.py
_tmp_*.txt
_fix_*.py
_migrate_*.py
beacon_comparison.txt
output.txt
release_log.txt
CRASH_ANALYSIS.md
node_modules

# Web Store — dados locais (nunca versionar)
**/webstore.log
plugin/arkshop_web/admin_steamids.json
**/settings.json

# Pontos/grupos legacy de jogadores (dados sensíveis)
plugin/CustomShop/configs/jogadores_pontos_grupos.csv
